"use client";

export default function EditorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // No need for special styling or structure
  // Just provide a layout to ensure proper routing
  return <>{children}</>;
} 